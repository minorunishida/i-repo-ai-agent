## 1. Implementation
- [x] 1.1 `src/app/agent/page.tsx` を作成し、`/agent` パスでエージェント1にリダイレクト
- [x] 1.2 `src/app/page.tsx` のスレッド初期化処理を修正：既存スレッドがない場合は自動作成
- [x] 1.3 `src/app/page.tsx` で存在しないエージェントの場合の処理を追加（適切なURLにリダイレクト）
- [x] 1.4 メッセージ保存ロジックが確実に動作することを確認（重複チェックは既に適切に実装済み）

## 2. QA
- [x] 2.1 `/agent` にアクセスして `/agent/1` にリダイレクトされることを確認
- [x] 2.2 `/agent/6` など存在しないエージェントにアクセスして適切に処理されることを確認
- [x] 2.3 初回アクセス時にサイドバーにスレッドが表示されることを確認
- [x] 2.4 メッセージを送信後、サイドバーに会話履歴が表示されることを確認
- [x] 2.5 スレッドタイトルが最初のメッセージ内容に自動更新されることを確認
- [x] 2.6 エージェントを切り替えても同じスレッド履歴が表示されることを確認
- [x] 2.7 ブラウザをリロードしても会話履歴が保持されることを確認

