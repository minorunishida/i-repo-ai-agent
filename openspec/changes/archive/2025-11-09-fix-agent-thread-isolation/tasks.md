## 1. Implementation
- [x] 1.1 `Thread`インターフェースに`agentId`フィールドを追加
- [x] 1.2 `createThread()`に`agentId`パラメータを追加
- [x] 1.3 スレッド作成時に`agentId`を保存
- [x] 1.4 スレッド選択時にそのスレッドの`agentId`にエージェントを切り替える
- [x] 1.5 エージェント切り替え時に、そのエージェント用のスレッドを探すか、新規作成する
- [x] 1.6 `AgentPicker`コンポーネントに`disabled`プロパティを追加
- [x] 1.7 `page.tsx`でメッセージがある時に`AgentPicker`を無効化する処理を追加
- [x] 1.8 動作確認（会話開始後はエージェント切り替えが無効化され、履歴スレッド選択時にエージェントが復元されることを確認）

